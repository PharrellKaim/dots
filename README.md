## Installation Guide

1. Install nix on your device

2. Clone the Repository

3. Activate home manager with following command: "nix run home-manager/master -- init --switch ."

4. Switch home Manager files with following command: "nix run home-manager/master -- switch -v --flake ."
